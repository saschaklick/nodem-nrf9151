Cargo.lock
/build/
/build-rtt/
/nodem-ffi/target/*

# Holds whatever device-reconfiguration commands you're currently using
# with `make reconfigure` (see the Makefile and scripts/reconfigure.sh) -
# typically a real registration code and/or device name, not something to
# commit. See reconfigure.cmds.example for the format.
/reconfigure.cmds
