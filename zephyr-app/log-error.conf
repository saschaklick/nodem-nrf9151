# Opt-in build fragment: drop debug()/info()/warn()-level logging, keep only
# error(). See zephyr-app/Kconfig (APP_LOG_LEVEL choice) and log.h. Applied
# via `LOG_LEVEL=error make build` (see the Makefile).
CONFIG_APP_LOG_LEVEL_ERROR=y
