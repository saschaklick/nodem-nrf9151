# Opt-in build fragment: drop debug()/info()-level logging, keep warn/error.
# See zephyr-app/Kconfig (APP_LOG_LEVEL choice) and log.h. Applied via
# `LOG_LEVEL=warn make build` (see the Makefile).
CONFIG_APP_LOG_LEVEL_WARN=y
