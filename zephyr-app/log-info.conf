# Opt-in build fragment: drop debug()-level logging, keep info/warn/error.
# See zephyr-app/Kconfig (APP_LOG_LEVEL choice) and log.h. Applied via
# `LOG_LEVEL=info make build` (see the Makefile).
CONFIG_APP_LOG_LEVEL_INFO=y
