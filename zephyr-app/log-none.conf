# Opt-in build fragment: drop all of debug()/info()/warn()/error() logging
# and log_bytes(). See zephyr-app/Kconfig (APP_LOG_LEVEL choice) and log.h.
# Applied via `LOG_LEVEL=none make build` (see the Makefile).
CONFIG_APP_LOG_LEVEL_NONE=y
